{
  "name": "classroom-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend && npm install --prefix backend && npx prisma generate --schema=backend/prisma/schema.prisma",
    "start": "npm start --prefix backend",
    "dev": "npm run dev --prefix frontend & npm run dev --prefix backend"
  }
}
